{
    "name": "visitor-counter",
    "version": "1.0.0",
    "description": "Visitor Counter App for GKE Deployment",
    "main": "server.js",
    "scripts": {
      "start": "node server.js",
      "dev": "nodemon server.js",
      "build": "echo 'Build step not required' && exit 0",
      "test": "echo \"No tests specified\" && exit 0"
    },
    "dependencies": {
      "express": "^4.18.2"
    },
    "engines": {
      "node": ">=18.0.0"
    }
  }
